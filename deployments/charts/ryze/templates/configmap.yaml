apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ryze.fullname" . }}-cm
  labels:
    {{- include "ryze.labels" . | nindent 4}}
data:
  config.yaml: |-
{{- if .Values.config }}
{{ tpl .Values.config . | indent 4 }}
{{- end }}